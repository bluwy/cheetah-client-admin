mutation AdminCreate(
  $username: String!
  $password: String!
  $privilege: AdminPrivilege!
) {
  createAdmin(
    data: { username: $username, password: $password, privilege: $privilege }
  ) {
    id
    createdAt
    updatedAt
    username
    privilege
  }
}
