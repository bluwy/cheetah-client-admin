#import "./fragments/Staff.graphql"

mutation StaffCreate(
  $username: String!,
  $fullName: String!,
  $password: String!
) {
  createStaff(
    data: {
      username: $username,
      fullName: $fullName,
      password: $password
    }
  ) {
    ...staffFragment
  }
}
