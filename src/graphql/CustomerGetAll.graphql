#import "./fragments/Customer.graphql"

query CustomerGetAll(
  $offset: Int,
  $limit: Int,
  $orderBy: CustomerOrderByInput,
  $where: CustomerWhereInput
) {
  customers(
    skip: $offset,
    first: $limit,
    orderBy: $orderBy,
    where: $where
  ) {
    ...customerFragment
  }
}
