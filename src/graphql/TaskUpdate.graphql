#import "./fragments/Task.graphql"

mutation TaskUpdate($id: ID!, $type: TaskType, $remarks: String) {
  updateTask(
    data: {
      type: $type,
      remarks: $remarks
    },
    where: {
      id: $id
    }
  ) {
    ...taskFragment
  }
}
