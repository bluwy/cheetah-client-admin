query GetJobs(
  $sortBy: JobSortBy,
  $sortDesc: Boolean,
  $limit: Int,
  $offset: Int
) {
  jobs(
    sortBy: $sortBy,
    sortDesc: $sortDesc,
    limit: $limit,
    offset: $offset
  ) {
    id
    customer {
      id
      name
    }
    dateIssued
  }
}
