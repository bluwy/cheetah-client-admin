#import "./fragments/Job.graphql"

query JobGetAll(
  $offset: Int,
  $limit: Int,
  $orderBy: JobOrderByInput,
  $where: JobWhereInput
) {
  jobs(
    skip: $offset,
    first: $limit,
    orderBy: $orderBy,
    where: $where
  ) {
    ...jobFragment
  }
}
