#import "./fragments/Job.graphql"

query JobAssignmentCreateGet($id: ID!) {
  job(where: { id: $id }) {
    id
    customer {
      id
      code
      name
      addresses
      staffPrimary {
        id
      }
      staffSecondary {
        id
      }
    }
  }
}
