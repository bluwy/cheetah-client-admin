mutation AuthAdminLogin($username: String!, $password: String!) {
  loginAdmin(username: $username, password: $password) {
    success
    message
    token
    userData {
      id
      role
      privilege
    }
  }
}
