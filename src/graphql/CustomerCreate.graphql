#import "./fragments/Customer.graphql"

mutation CustomerCreate(
  $code: String!,
  $name: String!,
  $addresses: [String!],
  $phoneNumber: String,
  $companyBelongId: ID!
  $email: String,
  $temporary: Boolean,
  $active: Boolean,
  $staffPrimaryId: ID!,
  $staffSecondaryWhere: StaffWhereUniqueInput
) {
  createCustomer(
    data: {
      code: $code,
      name: $name,
      addresses: $addresses,
      phoneNumber: $phoneNumber,
      companyBelong: { id: $companyBelongId },
      email: $email,
      temporary: $temporary,
      active: $active,
      staffPrimary: { id: $staffPrimaryId },
      staffSecondary: $staffSecondaryWhere
    }
  ) {
    ...customerFragment
  }
}
