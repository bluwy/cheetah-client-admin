#import "./fragments/Admin.graphql"

mutation AdminCreate(
  $username: String!,
  $password: String!,
  $privilege: AdminPrivilege!
) {
  createAdmin(
    data: {
      username: $username,
      password: $password,
      privilege: $privilege
    }
  ) {
    ...adminFragment
  }
}
