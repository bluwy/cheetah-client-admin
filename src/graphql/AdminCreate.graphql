mutation AdminCreate(
  $username: String!,
  $password: String!,
  $privilege: AdminPrivilege
) {
  createAdmin(
    username: $username,
    password: $password,
    privilege: $privilege
  ) {
    success
    message
    admin {
      id
      username
      privilege
    }
  }
}
