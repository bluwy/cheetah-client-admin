mutation AssignmentUpdate(
  $id: ID!,
  $checkIn: Date,
  $checkOut: Date,
  $needsFollowUp: Boolean
) {
  updateAssignment(
    id: $id
    checkIn: $checkIn,
    checkOut: $checkOut,
    needsFollowUp: $needsFollowUp
  ) {
    success
    message
  }
}
