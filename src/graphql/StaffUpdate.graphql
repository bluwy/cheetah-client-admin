#import "./fragments/Staff.graphql"

mutation StaffUpdate(
  $id: ID!,
  $username: String,
  $fullName: String
) {
  updateStaff(
    data: {
      username: $username,
      fullName: $fullName
    },
    where: {
      id: $id,
    }
  ) {
    ...staffFragment
  }
}
