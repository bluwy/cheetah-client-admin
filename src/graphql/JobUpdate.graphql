#import "./fragments/Job.graphql"

mutation JobUpdate(
  $jobId: ID!,
  $customerId: ID,
  $needsFollowUp: Boolean
) {
  updateJob(
    data: {
      customer: { id: $customerId },
      needsFollowUp: $needsFollowUp
    },
    where: {
      id: $jobId
    }
  ) {
    ...jobFragment
  }
}
