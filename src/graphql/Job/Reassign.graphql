mutation JobReassign($id: ID!, $data: ReassignJobInput!) {
  reassignJob(id: $id, data: $data) {
    oriJob {
      id
      state
    }
    newJob {
      id
      createdAt
      updatedAt
    }
  }
}
