#import "./fragments/Task.graphql"

mutation TaskCreate($assignmentId: ID!, $type: TaskType!, $remarks: String!) {
  createTask(
    data: {
      type: $type,
      remarks: $remarks
    },
    assignmentWhere: {
      id: $assignmentId
    }) {
    ...taskFragment
  }
}
