#import "./fragments/Assignment.graphql"

mutation AssignmentCreate(
  $address: String!,
  $staffPrimaryId: ID!,
  $staffSecondaryWhere: StaffWhereUniqueInput,
  $tasks: [TaskCreateInput!]!
  $jobId: ID!
) {
  createAssignment(
    data: {
      address: $address,
      staffPrimary: { id: $staffPrimaryId },
      staffSecondary: $staffSecondaryWhere,
      tasks: $tasks
    },
    jobWhere: {
      id: $jobId
    }
  ) {
    ...assignmentFragment
  }
}